spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ineuc-db
    username: root
    password: senha123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
    show-sql: true
api:
  security:
    token:
      secret: 55789854
